// Initialization for ES Users
import {
  Modal,
  Ripple,
  initTE,
} from "tw-elements";

initTE({ Modal, Ripple });

/* 
// Crear un botón
var nuevoBoton = document.createElement("button");

// Configurar propiedades del botón (opcional)
nuevoBoton.textContent = "Mi Nuevo Botón";
nuevoBoton.className = "mi-clase-de-estilo";

// Agregar el botón al DOM (por ejemplo, al cuerpo del documento)
document.body.appendChild(nuevoBoton);

// Agregar un evento al botón (opcional)
nuevoBoton.addEventListener("click", function() {
  alert("¡Haz clic en el nuevo botón!");
});


 */
/* 
`<button onclick="miFunc()">Haz click</button>`

<script>
  function miFunc() {
    alert('Se ha dado clic al botón!');
  }

   */